# frozen_string_literal: true

RSpec.describe BlingApi do
  it "has a version number" do
    expect(BlingApi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
